# Proxy Share

Proxy Share 管理当前 Windows 用户的系统代理，并在接管期间保护接管前的代理配置。

## Language

**代理接管**：Proxy Share 临时管理系统代理、保留接管前配置，并仅在当前配置仍等于其预期配置时提供恢复；外部修改会使恢复快照失效。
_避免使用_：代理开关、配置覆盖

**代理端点**：用于手动系统代理的、已验证的主机地址与端口组合。
_避免使用_：代理文本、地址字符串
